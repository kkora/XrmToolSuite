# Everything the UI tests capture or export lands here (screenshots + exported reports),
# organized into per-date/per-tool subfolders and regenerated each run — never tracked.
*
!.gitignore
!.gitkeep
